{
  "name": "synergy-brand-architect",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp -r css images js fonts *.html dist/ && cd admin && npm install && CONVEX_URL=$NEXT_PUBLIC_CONVEX_URL npx convex codegen && npm run build && cd .. && mkdir -p dist/admin && cp -r admin/out/* dist/admin/"
  },
  "dependencies": {
    "convex": "^1.11.0"
  }
}
